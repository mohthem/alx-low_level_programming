#include "main.h"
/**
 * _abs - cheks lowercase letters!.
 *
 *@a: input to be tested.
 *
 * Return: 0 on success
 */
int _abs(int a)
{
	if (a < 0)
	{

		return (-1 * a);
		_putchar('+');
	}
	return (a);
}
