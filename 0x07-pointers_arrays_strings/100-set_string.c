#include <stdio.h>

/**
 * set_string - adds the diagonal elements of mat.
 * @s: pointer to hold char;
 * @to: a char that contains the chracter.
 * Return: nothing.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
